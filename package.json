{
  "name": "backbone",
  "version": "1.0.0",
  "description": "This is the view when the SAJARI check is in progress",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.14.0"
  }
}
